services:
  trial_docker:
    build:
      context: .
      dockerfile: Dockerfile
    image: trial_docker:v1
    working_dir: /app
    command: bash -c "python flask_app.py"
    ports:
      - "5000:5000"